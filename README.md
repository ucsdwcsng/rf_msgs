# rf_msgs
RF sensor msgs for ROS

A catkin package that provides CSI and AOA messages. Place it in the 'src' folder in your catkin workspace, then the messages can be included the same way sensor_msgs and std_msgs can be included